% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exactt.R
\name{exactt.wald}
\alias{exactt.wald}
\title{Exact Wald-test}
\usage{
exactt.wald(
  model,
  data,
  alpha = 0.05,
  variables = NULL,
  beta0 = c(0, NULL),
  nBlocks = 5,
  nPerms = NULL,
  studentize = TRUE,
  optimize = FALSE,
  seed = 31740,
  ...
)
}
\arguments{
\item{model}{A formula specifying the model.}

\item{data}{A data frame or matrix containing the variables used in the model.}

\item{alpha}{The significance level used for the hypothesis tests; defaults to 0.05.}

\item{variables}{Optional; a character vector of predictor names to test.
If NULL, all predictors in the model are tested.}

\item{beta0}{If 0, test whether the variables of interest are equal to the zero vector.
If NULL, creates a grid of beta0 values (for confidence intervals) and tests the variables of interest.}

\item{nBlocks}{The number of blocks to use for block permutations.}

\item{nPerms}{Optional; the number of permutations to perform.
If NULL or greater than the number of possible permutations, all permutations are used.}

\item{studentize}{Logical indicating whether to use studentized residuals for the test.}

\item{optimize}{Logical indicating whether to optimize the ordering of the data.}

\item{seed}{Seed used when optimizing using \code{GA::ga()}. Default is 31740.}

\item{...}{Additional arguments passed to \code{GA::ga()} for optimizing power.
This can include parameters like \code{popSize}, \code{maxiter}, \code{parallel}, etc.,
that are used to configure the genetic algorithm. Note that when sample size is large
optimizing is computationally expensive and has little effect.}
}
\value{
An object of class 'exactt', which includes:
\itemize{
\item \code{summary}: A matrix summarizing the test results for each variable.
\item \code{detailed}: A list containing detailed test results for each variable.
\item \code{gaResults}: Optional; a list of results from the \code{GA::ga()} function, included only when
power optimization is performed via genetic algorithm parameters. Each element of the list
corresponds to results for one of the tested variables, containing details like the best
permutations found, fitness scores, and other GA diagnostics.
\item \code{call}: The matched call.
}
}
\description{
Exact Wald-test
}
\details{
The function divides the data into blocks specified by \code{nBlocks} and performs permutations
within across blocks to generate the null distribution of the test statistic. The user can
specify a set number of permutations with \code{nPerms}, or allow the function to calculate all
possible permutations if \code{nPerms} is unspecified or too large.

If \code{studentize} is TRUE, studentized residuals are used to adjust the test statistics,
potentially leading to more robust inference under model misspecification.

The function allows for a high degree of customization through its parameters and can
handle large datasets and complex model structures efficiently.
}
